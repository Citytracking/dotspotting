	<input type="text" size="40" name="label" />
	<br />
	<label for="label">Give your upload a name (optional)</label>
	<br /><br />

	{if $cfg.enable_feature_dots_indexing}

	<input type="text" size="40" name="dots_index_on" />
	<br />
	<label for="dots_index_on">A comma-separated list of the field names you would like us to index (optional, maximum of {$cfg.dots_indexing_max_cols|escape})</label>
	<br /><br />

	{/if}

	{if $import_formats}

	<label for="mime_type">By default Dotspotting will try to guess what kind of file you're trying to upload but you can also tell it yourself:</label>

	<br /><br />

<select name="mime_type">
<option>Select a file type</option>
{foreach from=$import_formats item="mime_type" key="extension"}
{capture assign="label"}{if $extension=='xls'}Excel{else}{if $extension=='rss' or $extension=='json'}Geo{/if}{$extension|@strtoupper}{/if}{/capture}
<option value="{$mime_type|escape}">{$label|escape}</option>
{/foreach}
</select>

	<br /><br />
	{/if}

	<input type="checkbox" name="private" />
	<label for="private">make all these dots private</label>
	<br /><br />
